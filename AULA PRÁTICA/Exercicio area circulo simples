#include <stdio.h>
#include <math.h>
int main() {
float raio, area;
printf("Digite o raio: \n");
scanf("%f", &raio);
area = PI * pow(raio, 2);
printf("Área total do círculo: %g \n", area);
return 0;
}
