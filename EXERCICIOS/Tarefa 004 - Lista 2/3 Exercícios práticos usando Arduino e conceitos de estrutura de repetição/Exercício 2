analogRead(A1):A função analogRead(A1) lê o valor de um pino analógico do Arduino, no caso, opino A1.Essa função converte o valor da tensão no pino analógico (que varia de 0 a 5V) paraum valor digital entre 0 e 1023. Isso porque a entrada analógica no Arduino é lidacom uma resolução de 10 bits, ou seja, 1024 valores possíveis (de 0 a 1023).cameraData[3]:cameraData é um vetor (ou array) onde o índice [3] se refere à quarta posição dovetor, porque os índices começam em 0 (logo, cameraData[0] é o primeiroelemento, cameraData[1] é o segundo, e assim por diante).cameraData[3] armazenará o valor retornado pela função analogRead(A1)
